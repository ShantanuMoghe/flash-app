import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flashapp.app',
  appName: 'flash-app',
  webDir: 'dist/flash-app/browser'
};

export default config;
